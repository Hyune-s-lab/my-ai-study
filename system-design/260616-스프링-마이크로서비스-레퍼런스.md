# 참고 — 스프링 클라우드 마이크로서비스 레퍼런스 아키텍처

> 출처: 『스프링 마이크로서비스 코딩 공작소』의 종합 예제 아키텍처. **`diagrams`(mingrammer) 코드 → PNG**로 그렸다. 생성: [`assets/스프링-마이크로서비스-레퍼런스.py`](./assets/스프링-마이크로서비스-레퍼런스.py).
> Spring·Kafka·Redis·Elasticsearch·Kibana·Logstash·Grafana·Prometheus는 라이브러리 내장 아이콘. **Keycloak·Zipkin은 diagrams에 없어 박스**로 표기.

![스프링 클라우드 마이크로서비스 레퍼런스 아키텍처](./assets/스프링-마이크로서비스-레퍼런스.png)

## 구성 요소

| 영역 | 컴포넌트 | 역할 |
|---|---|---|
| **엣지/인프라** | 서비스 게이트웨이 (Spring Cloud Gateway) | 단일 진입점 · 라우팅 · 토큰 검증 · 필터 |
| | 서비스 디스커버리 (Eureka) | 서비스 등록/조회 → 동적 라우팅 |
| | 구성 서비스 저장소 (Spring Cloud Config) | 외부화된 설정 중앙 관리 |
| | 키클록 (Keycloak) | 인증 · OAuth2/OIDC 토큰 발급 |
| **비즈니스** | 조직 서비스 / 라이선싱 서비스 | 도메인 서비스. 각각 **Resilience4j**(CB·Retry·Bulkhead) 탑재 |
| | 조직 DB / 라이선싱 DB | 서비스별 전용 DB (DB per service) |
| | Redis | 라이선싱 조회 캐시 |
| | Kafka 토픽 | 조직 서비스 **발행** → 라이선싱 서비스 **구독** (비동기 이벤트) |
| **관측(도커)** | Zipkin | 분산 추적(trace) |
| | Logstash → Elasticsearch → Kibana | 로그 수집·저장·질의 (ELK) |
| | Prometheus(Actuator) → Grafana | 메트릭 수집·대시보드 |

## 우리가 본 개념과의 연결

- **게이트웨이**: [모의면접 타임아웃](../cs-study/260610-모의면접-타임아웃.md)의 inbound/outbound 양면이 그대로 — 클라이언트 hop + 다운스트림 서비스 hop.
- **Resilience4j**: 타임아웃 문서의 Circuit Breaker·Retry·Bulkhead가 각 서비스에 박혀 있는 형태.
- **Redis**: [모의면접 Redis](../cs-study/260610-모의면접-레디스.md)의 조회 캐시 용도.
- **Kafka 발행/구독**: 서비스 간 비동기 통신 → 결합도↓·장애 격리. (종소세 설계의 SQS 잡 큐와 같은 결: 동기 호출을 이벤트로 분리)
- **관측 3종**: 추적(Zipkin)·로그(ELK)·메트릭(Prometheus/Grafana) — 분산 시스템 디버깅의 표준 삼각형.

## 운영 셋업 메모 (그림 밖에서 해야 하는 일)

그림엔 박스·화살표만 있지만, 실제로 돌리려면 **인증·연결 셋업**이 필요하다. 대표적인 것:

- **Grafana ↔ Prometheus 연결**: Grafana에 Prometheus를 **데이터소스로 등록**(URL + 인증). 인증은 보통 **Grafana 서비스 계정 토큰**(Service Account Token, 구 API Key 대체)으로 한다. 대시보드·데이터소스를 코드로 관리하려면 **provisioning**(YAML)로 토큰·데이터소스를 주입.
- **Prometheus 스크레이프**: 각 서비스의 `/actuator/prometheus` 엔드포인트를 `scrape_config`에 등록(또는 ServiceMonitor). 노출 엔드포인트엔 인증/네트워크 제한.
- **Keycloak**: realm·client 생성, client secret 발급, 게이트웨이가 토큰 검증용 **JWK(공개키) URL** 설정.
- **Eureka/Config**: 서비스가 Eureka에 등록할 자격, Config 서버 접근(보안 설정 시 토큰/계정).
- **Kafka**: 토픽 생성, 컨슈머 그룹, (운영) SASL/TLS 인증.
- **Redis/DB**: 접속 비밀번호·시크릿(보통 Secret Manager/Vault), TLS.

> 즉 "박스를 잇는 화살표 = 인증·시크릿·연결 설정이 깔려 있다는 전제". 다이어그램엔 안 그리지만 **운영 체크리스트로 따로 적어둔다.** (예: Grafana 토큰 발급 → 데이터소스 provisioning → 대시보드 import)

## 메모

- 이 그림은 **개념 학습용 레퍼런스**다. 실서비스에선 Eureka/Config 대신 K8s 서비스 디스커버리·ConfigMap, Zipkin 대신 OpenTelemetry+Tempo 등으로 대체되기도 한다.
- 핵심은 박스 이름이 아니라 **역할의 분리**: 진입(게이트웨이) · 발견(디스커버리) · 설정(구성) · 인증(키클록) · 도메인(서비스+회복탄력성) · 비동기(Kafka) · 관측(추적·로그·메트릭).
