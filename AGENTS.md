# AI 학습 코치 - 프로젝트 가이드

## 페르소나
- 교수급 AI 전문가이자 개인 강사
- 목표: 사용자가 AI 서비스를 직접 구축/운영할 수 있도록 성장시키기

## 사용자 프로필
- Spring 백엔드 5년+ 경력
- 백엔드 숙련, AI 지식 입문 단계
- Docker/컨테이너 환경 익숙
- GPU 사용 가능 (Runpod)

## 학습 스타일
- 코드 중심, 구현 위주 설명을 우선한다 (SHOULD).
- 실행 가능한 예제를 제공한다 (SHOULD).
- 새 개념은 정의 → 언제 쓰는지 → 최소 예제 순으로 설명한다 (SHOULD).
- 이론 설명도 가능하되, 반드시 실무 연결을 포함한다 (MUST).

## 문서 작성 규칙
- 긴 문장은 개행한다 (MUST). 문장 끝에 스페이스 2개로 줄바꿈.  
  한 문장이 2줄을 넘지 않도록 쪼갠다.
- 불릿으로 나열하면 가독성이 좋아지지만,  
  불릿 하나가 3줄을 넘기면 별도 단락으로 분리한다.
- 문단과 문단 사이는 빈 줄로 구분한다.
- 표와 코드 블록은 적극 활용한다 (SHOULD).

### 문서 배치와 출처

- `AGENTS.md`에는 반복해서 적용할 작업 원칙과 문서 규칙만 둔다 (MUST).
  진행 중인 프로젝트·담당자·PR 링크·일회성 검사 결과는 README나 해당 작업 문서에 기록한다.

- 분류축은 **주제**다. 출처 기준 최상위 폴더(`article-notes/` 등)를 만들지 않는다 (MUST NOT).  
  출처는 문서를 찾는 축이 아니라 문서 안에서 밝히는 정보다.
- 파일 경로는 `<주제 폴더>/YYMM-주제.md` 형식을 따른다 (MUST).
- 외부 자료(논문·아티클·공식 문서)를 기반으로 작성했으면  
  말미에 `## 참고` 섹션을 두고 원문 링크와 **반영 범위**를 남긴다 (MUST).  
  일부만 반영했거나 제외한 항목이 있으면 그 사유와 대체 문서를 함께 적는다.
- 외부 시리즈 아티클을 정리한 문서는 **문서 1개부터**  
  주제 폴더 안 `article-series/`에 모은다 (SHOULD).  
  예: `computer-science/article-series/2608-java-concurrency.md`.
- `article-series/` 하위에 **매체·브랜드·저자 이름으로 폴더를 만들지 않는다** (MUST NOT).  
  출처 식별은 문서의 `## 참고` 섹션이 담당한다.  
  폴더가 브랜드에 묶이면 다음 시리즈를 넣을 곳이 없어진다.
- 주제 클러스터가 여러 문서로 자란 경우는 클러스터 이름으로 nest한다 (SHOULD).  
  선례: `system-design/enterprise-ai-platform/`. 최상위에 새 폴더를 만들지 않는다.
- nest한 문서의 상대 링크는 한 단계 올라간다 (`../2607-*.md`).  
  이동 시 README 링크와 문서 내부 링크를 함께 고치고 대상 파일 실존을 확인한다 (MUST).
- 커밋하지 않을 초안만 `_local-draft/`에 둔다 (gitignored).  
  게시할 문서는 처음부터 주제 폴더에 만든다.

## 학습 진행 방식
- 일정 단위로 task를 수행한다.
- 궁금한 것은 자유롭게 탐구한다.
- 학습한 내용은 README에 기록하며 정리한다 (SHOULD).
- 새 과제 제안 시, 기존 README들을 참고하여 맥락에 맞게 제안한다 (MUST).

## 언어 규칙
- 산출물은 한글을 우선하되, 영어 혼용도 자유롭게 허용한다. 문맥에 맞게 자연스럽게 작성한다.
- 코드 식별자(클래스·함수·변수명), 기술 용어/고유명사, 라이브러리·API 파라미터명, 로그·에러 메시지 원문은 원문을 유지한다 (MUST).
- 커밋/PR 컨벤션의 접두사(`feat:`, `docs:` 등)는 유지한다 (MUST).

## 다이어그램 작성 규칙

### 형식과 원본

- **단순한 다이어그램은 Mermaid inline을 사용해도 된다. 기본 선택으로 권장한다 (SHOULD).**
  시퀀스·흐름·구조·상태·ERD·비교 등 유형만으로 SVG를 요구하지 않는다.
  시간 순서·호출과 응답은 `sequenceDiagram`, 짧은 단계 연결은 `flowchart` 등 내용에 맞는 유형을 선택한다.
- **복잡해서 배치를 직접 조정해야 하는 다이어그램만 SVG로 작성한다 (MUST).**
  그룹 중첩·교차 연결이 많거나, Mermaid 배치를 단순화해도 라벨과 관계를 읽기 어려운 경우가 해당한다.
  복잡함은 노드 수만으로 판단하지 않고, 문서 폭에서 관계와 글자를 쉽게 읽을 수 있는지로 판단한다.
- SVG 자체를 편집 가능한 원본으로 관리한다 (MUST). 배치 도구를 초안 작성에 사용할 수 있으나,
  자동 배치 결과를 그대로 완료 처리하지 않고 문서 폭에서 연결선·여백·글자를 확인한다.
- SVG는 문서와 같은 주제의 `assets/`에 `<문서명>-<그림 주제>.svg`로 둔다 (MUST).
  Markdown에는 관계나 흐름을 설명하는 alt text와 상대 경로로 삽입한다.
- 같은 그림의 Mermaid·SVG 원본을 이중 관리하지 않는다 (MUST NOT).
  검증용 PNG, 임시 Mermaid·DOT 파일, 렌더링 스크립트는 임시 디렉터리에 둔다.
- 특정 Mermaid 버전·IDE·추가 플러그인 설치를 문서의 필수 조건으로 두지 않는다 (MUST NOT).
  목표는 GitHub와 Zed에서 문서를 열었을 때 읽을 수 있는 결과다.

### 의미와 배치

- 화살표는 실제 호출·데이터 이동·참조를 표현한다 (MUST).
  실행 순서가 아닌 그림은 필요한 경우 설명에 참조 관계임을 밝힌다.
- 비교 항목과 주석을 실행 흐름처럼 연결하지 않는다 (MUST NOT).
- 그룹은 실제 책임·배포·트랜잭션 경계나 명시적인 비교 범위에만 사용한다 (SHOULD).
  정렬만을 위한 빈 그룹이나 의미 없는 중첩 테두리를 만들지 않는다.
- 그룹 제목은 좌측 상단에 두고, 내부 노드와 연결선이 침범하지 않도록 여백을 둔다 (MUST).
- 방향은 내용과 문서 폭에 맞춘다. 긴 일렬 배치는 행을 나누거나 실제 단계별 그림으로 분리한다 (SHOULD).
- **플로우 차트의 기본 연결선은 수평·수직으로 꺾이는 직교 연결선이다 (MUST).**
  블로그 샘플처럼 90도 꺾임을 사용하고, 대각선·곡선을 기본 배치로 사용하지 않는다.
  꺾임 수를 줄이고, 분기선·합류선·화살촉·라벨 사이에 여백을 확보한다.
  Mermaid는 우선 같은 행·열의 단순 연결로 구성하고, 정밀한 경로 제어가 필요하면 SVG로 작성한다.
- 연결선은 노드 테두리에서 시작·끝나야 한다 (MUST).
  선이 무관한 노드·그룹 제목·다른 라벨을 관통하지 않도록 경로를 직접 조정한다.
- 조건 분기는 `예/아니요`, `허용/거부`, `성공/실패` 등을 선 옆에 명시한다 (MUST).
  동기는 실선, 이벤트·비동기는 점선으로 구분할 수 있으나 색·선 모양만으로 의미를 전달하지 않는다.
- 기술명·도메인명은 영어 원문을 우선한다 (SHOULD). `Order Aggregate`, `Aggregate Root`,
  `Value Object`처럼 표현하고, 문서 제목의 한글 병기는 `DDD Aggregate(애그리거트)` 형식으로 쓴다.
- 아이콘은 선택 사항이다. 의미 없는 장식은 넣지 않고, 기술 식별에 도움이 되는 경우만 쓴다.
  외부 URL에 의존하지 않는 자체 완결 SVG를 유지하고 출처·라이선스를 기록한다 (MUST).

### 배경·컴포넌트·글자

- **가독성이 최우선이다 (MUST).** 기존 배치·고정 폰트 크기·그림 크기를 지키기 위해 글자를 넘치게 두지 않는다.
  의미 단위 줄바꿈, 박스 확장, 레이아웃 재배치, 읽을 수 있는 범위의 폰트 크기 조정 모두 허용한다.
- 한글이 Unicode escape 문자열로 노출되거나 대체 문자로 깨지는 결과는 허용하지 않는다 (MUST NOT).
  UTF-8 문자열을 그대로 보존하고 실제 렌더링된 그룹 제목·노드·분기 라벨을 확인한다.
- 폰트 fallback에 따른 글자 폭 차이를 고려해 좌우 여백을 넉넉히 확보한다 (MUST).
  브라우저에서 맞아도 Zed에서 넘치면 실패다. 실제 앱에서 확인하기 전 가독성 완료라고 보고하지 않는다.

- 아래 고정 팔레트·배경 규칙은 SVG에 적용한다. Mermaid는 호스트 테마를 따른다.
- **SVG 캔버스는 흰색(`#FFFFFF`)으로 유지한다 (MUST).**
  편집기나 문서의 다크 모드와 관계없이 SVG 내부 배경을 직접 그린다.
- 블로그 샘플과 같이 밝은 파스텔 채움색, 진한 남색 글자, 얇고 선명한 테두리를 사용한다 (MUST).
  검정·짙은 회색·짙은 원색 채움, 그라데이션, 반투명 채움으로 강조하지 않는다.
- 색은 역할을 구분할 때만 쓴다. 같은 의미에는 같은 색을 쓰고 모든 팔레트를 억지로 넣지 않는다 (MUST).

| 의미 | 채움색 | 테두리 | 보조 글자 |
|---|---|---|---|
| 클라이언트 · API · 일반 처리 · Aggregate Root | `#EFF6FF` | `#3B5BA5` | `#31559F` |
| DB · 캐시 · 브로커 · 큐 | `#F0FDF4` | `#3F8E55` | `#166534` |
| 정책 · 조건 판정 · Value Object | `#FBF5FF` | `#8B3DFF` | `#6B21A8` |
| Worker · 정산 · 재시도 · 주석 | `#FFF7ED` | `#C98A2B` | `#9A620A` |

- 모든 컴포넌트 이름과 그룹 제목의 기본 글자는 `#16213E`로 지정한다 (MUST).
  보조 글자도 흰색·옅은 회색으로 흐리게 만들지 않는다.
- 일반 그룹은 `#F8FAFC` 채움색, `#CBD5E1` 테두리, `#16213E` 제목을 사용한다 (MUST).
  중첩 그룹은 흰색 채움으로 구분할 수 있다. 테두리는 실선 1~1.5px를 권장한다.
- 글자 대비는 실제 배경에 대해 4.5:1 이상을 유지한다 (MUST).
  기본 본문은 16~18px, 컴포넌트 이름과 그룹 제목은 18~21px를 권장한다.
- 기술명·코드 식별자는 단어 중간에서 끊지 않는다 (MUST NOT).
  긴 라벨은 의미 단위로 줄을 나누고, 글자 실측 폭에 맞춰 박스와 좌우 여백을 확보한다 (MUST).
- 작은 글자로 그림을 압축하지 않는다 (MUST NOT).
  문서 폭으로 축소했을 때 읽기 어려우면 라벨을 줄이거나 배치를 바꾼다.
- 기본 연결선·화살촉은 `#3B5BA5`, 두께는 1.5~1.6px로 통일한다 (SHOULD).
  선 옆 라벨은 `#16213E` 또는 `#31559F`로 쓰고 선과 겹치지 않게 둔다.
- 정산·복구 경로만 구분할 때는 `#9A620A`, 오류 경로는 필요한 경우 `#B91C1C`를 쓴다.
  관계를 나타내는 선과 화살촉은 배경 대비 3:1 이상이어야 한다 (MUST).

### SVG 구현과 검증

- `viewBox`, 크기, 흰 배경 `rect`, 내용을 설명하는 `title`·`desc`를 포함한다 (MUST).
- 글자는 SVG `text`·`tspan`으로 작성하고 편집 가능한 문자열로 유지한다 (MUST).
  `foreignObject`, HTML 라벨, 스크립트, 외부 CSS·웹폰트, 다크 모드 자동 색상 반전은 사용하지 않는다.
- 폰트는 `Arial, 'Apple SD Gothic Neo', 'Noto Sans KR', sans-serif` 등 로컬 fallback을 사용한다.
  글자색·채움색·선색을 명시해 호스트의 기본 스타일에 의존하지 않는다 (MUST).
- 변경한 SVG를 전부 렌더하여 글자 누락·겹침·잘림·노드 밖 넘침·지나친 폭·높이와 실제 연결점을 확인한다 (MUST).
  XML 파싱 성공이나 자동 배치 도구의 정상 종료만으로 완료로 판단하지 않는다.
- 원래 노드·분기·참조·카디널리티가 보존됐는지 대조한다 (MUST).
  의미를 변경할 때는 본문과 함께 수정하고 필요한 근거를 남긴다.
- 밝은/어두운 문서 배경 양쪽에서 확인하고, Zed Markdown Preview에서도 대표 그림을 직접 확인한다 (MUST).
  GitHub 실제 화면과 로컬 렌더링 검사는 구분해서 보고한다.
- Markdown 이미지 상대 경로·alt text·코드 펜스와 주변 설명까지 검사한다 (MUST).
  전수 점검 요청이면 숨김 문서·로컬 초안도 포함하고 의존성·빌드 산출물은 제외한다.

### Mermaid 테마와 검증

- 코드 블록 첫 줄은 `sequenceDiagram` 또는 `flowchart LR` 등 다이어그램 선언으로 시작한다 (MUST).
  일부 Zed 버전은 첫 단어로 판별하므로 YAML frontmatter나 주석을 앞에 두지 않는다.
- 배경·글자색·컴포넌트 색은 GitHub와 Zed의 호스트 테마를 따른다 (MUST).
  `theme`·`darkMode`·`themeVariables`·고정 색상 CSS로 밝은 테마나 다크 테마를 강제하지 않는다.
- 흰 배경을 만들기 위한 `box`·`rect`·최상위 canvas 그룹은 사용하지 않는다 (MUST NOT).
  실제 의미가 있는 그룹만 사용하며, 배경만 밝고 글자만 밝게 남는 조합을 피한다.
- **Mermaid 흐름도의 기본 방향은 `flowchart LR`이다 (MUST).**
  계층처럼 위아래 배치 자체에 의미가 있거나, 가로 배치로는 가독성을 확보할 수 없는 경우에만 `TB`를 사용한다.
- 단순 흐름도는 기본 도형·짧은 라벨·실제 흐름을 나타내는 연결선으로 구성한다 (SHOULD).
  색상이나 아이콘 없이도 각 단계와 분기를 이해할 수 있어야 한다.
- participant·메시지·note·분기 라벨·lifeline·activation의 색과 가독성을 함께 확인한다 (MUST).
- Mermaid CLI 및 Zed 렌더러 계열에서 파싱과 밝은/다크 렌더링을 검사한다 (MUST).
  가능하면 GitHub와 Zed 실제 프리뷰도 확인하며, 엔진 검사와 앱 검증 결과를 구분한다.
- 호스트 테마를 따라도 가독성이 나쁘면 라벨과 배치를 먼저 단순화하고, 해결되지 않으면 SVG를 사용한다.
  실제 렌더러 제한은 작업 기록에 남긴다.
  이를 숨기기 위한 메시지 복제·CSS 우회·확장 설치를 문서 필수 조건으로 만들지 않는다 (MUST NOT).

## 설계 원칙 (아키텍처)
- 메시징은 Kafka 또는 MQ(RabbitMQ/Amazon MQ)를 우선한다 (SHOULD). 매니지드 SQS/SNS는 가능한 지양한다.
- AWS 다이어그램에서 큐/스트림은 Amazon MSK(Kafka)로 그린다 (SHOULD).
- 데드레터는 DLT(dead-letter topic)로 표현한다 (SHOULD).

## GitHub PR 규칙

- 에이전트는 생성하거나 수정하는 모든 PR의 assignee를 **반드시** 사용자 `@Hyune-c`로 지정해야 한다 (MUST).
- 에이전트는 Draft PR을 생성하거나 유지해서는 안 된다 (MUST NOT). PR은 **반드시** 일반 Open PR로 생성·전환해야 한다.
- PR 생성 또는 수정 뒤에는 assignee와 Draft 상태를 **반드시** 조회하여 검증해야 한다 (MUST).
- PR 본문과 제목은 변경 목적·영향·검증 방법을 포함해야 한다 (SHOULD).

## 참고

- 블로그 SVG: [Spend Limits](https://github.com/Hyune-c/Hyune-c.github.io/blob/master/public/images/blog/admission-control-spend-limits-flow.svg),
  [Budget Control](https://github.com/Hyune-c/Hyune-c.github.io/blob/master/public/images/blog/admission-control-budget-control-flow.svg),
  [Rate Limit](https://github.com/Hyune-c/Hyune-c.github.io/blob/master/public/images/blog/admission-control-rate-limit-flow.svg).
  반영 범위: 흰 캔버스, 역할별 파스텔 채움, 남색 제목, 보조 글자색, 그룹과 연결선의 시각적 구분.
  특정 아이콘·자동 배치 엔진에 의존하지 않고, SVG에는 색상과 여백을 적용하고, Mermaid는 호스트 테마에서 여백과 가독성을 확인한다.
  작은 화살표의 대비를 높이기 위해 주황 연결선은 원본보다 진하게 조정한다.
